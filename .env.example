# Environment Configuration Template
# Copy this file to .env.local and customize with your values
# IMPORTANT: .env.local is gitignored - your real values stay private

# ==============================================================================
# SSH Configuration
# ==============================================================================

# Your SSH server details
SERVER_IP=192.0.2.10
SERVER_USER=your-username
SERVER_PORT=22

# SSH key path (Windows example)
SSH_KEY_PATH=C:\Users\YourName\.ssh\id_ed25519

# SSH key path (Linux/Mac example)
# SSH_KEY_PATH=~/.ssh/id_ed25519

# ==============================================================================
# Gitea/Git Server Configuration
# ==============================================================================

# Gitea SSH tunnel settings
GITEA_LOCAL_PORT=2222
GITEA_REMOTE_HOST=your-user@gitea.example.com
GITEA_REMOTE_PORT=2222
GITEA_VPN_CHECK_HOST=gitea.example.com

# Git repository URL (with tunnel)
GIT_REPO_URL=ssh://git@localhost:2222/username/repository.git

# ==============================================================================
# Windows Specific Settings
# ==============================================================================

# PowerShell execution policy (for reference, not used directly)
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Windows OpenSSH path
WINDOWS_SSH_PATH=C:\Windows\System32\OpenSSH\ssh.exe

# ==============================================================================
# Linux Specific Settings
# ==============================================================================

# Server hostname (if different from IP)
SERVER_HOSTNAME=server.example.com

# Backup directory
BACKUP_DIR=/home/your-username/backups

# Log directory
LOG_DIR=/var/log/custom-scripts

# ==============================================================================
# Optional: Additional Services
# ==============================================================================

# Database connection (example - customize as needed)
# DB_HOST=192.0.2.20
# DB_PORT=5432
# DB_NAME=myapp
# DB_USER=dbuser

# API keys (if needed for scripts)
# API_KEY=your-api-key-here
# API_SECRET=your-api-secret-here

# ==============================================================================
# Usage Instructions
# ==============================================================================
#
# 1. Copy this file:
#    cp .env.example .env.local
#
# 2. Edit .env.local with your real values:
#    nano .env.local
#    # or
#    notepad .env.local
#
# 3. Load in PowerShell:
#    Get-Content .env.local | ForEach-Object {
#        if ($_ -match '^([^#].+?)=(.+)$') {
#            [Environment]::SetEnvironmentVariable($matches[1], $matches[2], 'Process')
#        }
#    }
#
# 4. Load in Bash:
#    set -a; source .env.local; set +a
#
# 5. Use in scripts:
#    $ServerIP = $env:SERVER_IP  # PowerShell
#    SERVER_IP="${SERVER_IP}"    # Bash
#
# ==============================================================================
